defaults:
  - base_experiment  

max_n_frames: 500_000

on_policy_n_envs_per_worker: 4
on_policy_collected_frames_per_batch: 2000
off_policy_n_envs_per_worker: 4
off_policy_collected_frames_per_batch: 2000
evaluation_episodes: 16
evaluation_interval: 10_000
parallel_collection: True
evaluation_deterministic_actions: False
checkpoint_interval: 50_000

sampling_device: "cpu" 
train_device: "cuda"
buffer_device: "cuda"
render: false